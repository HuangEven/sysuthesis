% !TEX root = ../main.tex

\chapter{多 GPU 扩展与聚合优化}

\section{多 GPU 扩展需求分析}

第四章已经从单 GPU 视角完成了系统级优化，并证明在 GPU 主导端到端执行链路、异步 I/O---计算流水线和后端重构协同作用下，单卡场景中的主要损耗已不再集中在主机---设备边界，而更多落在设备侧检索与评分过程本身。然而，这并不意味着单卡已经足以覆盖后续所有实验和部署需求。随着输入规模继续增大、查询并发进一步提升、候选索引逼近单卡显存上限，单卡系统会同时遭遇三类上限：计算上限、容量上限和调度上限。它们对应的问题并不相同，因此多 GPU 扩展也不能被简单理解为“再增加几张卡”。

第一类需求来自计算上限。当候选召回和异常评分都已稳定驻留在 GPU 侧时，单卡吞吐最终会受限于单设备的计算资源。此时，即便继续压缩局部路径开销，系统整体 QPS 仍然难以随业务峰值线性提升。第二类需求来自显存与索引容量上限。在更大索引或更大规模合成扩展数据集条件下，单 GPU 可能无法同时容纳完整索引、副本缓存以及评分中间张量，系统必须在缩小索引规模、降低候选预算和增加设备数量之间进行权衡\citep{cuvs_multi_gpu_nn,cuvs_multi_gpu_ivf_host}。第三类需求则来自调度压力。金融近实时异常检测并不只追求平均吞吐，系统还需要在高峰时段保持稳定的延迟分位数和资源利用率；一旦请求在单设备上集中堆积，即使平均性能尚可，尾延迟也会迅速恶化。

因此，对本文系统而言，多 GPU 扩展的目标并不是抽象地提高并行度，而是在保持检测效果、延迟分位数和实现复杂度可控的前提下，分别回答三个问题：如何把更多查询稳定分摊到多张 GPU 上，如何在容量受限时继续维持索引可用性，以及如何避免调度与共享路径重新成为系统上限\citep{cuvs_multi_gpu_nn}。这些问题一旦进入多卡场景，局部结果如何归并、CPU 是否重新回到关键路径、共享存储路径是否发生拥塞，都会直接影响扩展收益是否能够兑现。

这也是第五章与第四章之间的分工边界所在。第四章解决的是“单卡链路能否被压缩到更低开销”，而第五章需要继续回答“在单卡热路径已经基本收紧之后，系统吞吐如何继续扩大而不重新引入新的中心瓶颈”。基于这一判断，本文后续优先采用数据并行与索引复制的 replicated 主方案，使每张 GPU 尽可能独立完成候选召回与异常评分；只有在索引容量或业务约束明确表明无法复制完整索引时，才进入 GPU 侧跨卡归并与设备间通信的兜底路径。换言之，本章关注的重点不是构造最复杂的多 GPU 检索系统，而是在金融近实时异常检测场景下找到更符合吞吐优先和工程可控性的扩展方式。

\section{多 GPU 扩展策略设计原则}

多 GPU 扩展方案的设计必须建立在系统目标与瓶颈分析的基础上。结合第四章单卡结论与第二章相关工作梳理，本文将多 GPU 扩展方案归纳为四项基本原则：查询级并行优于单查询跨卡并行；数据并行与索引复制优先于直接采用索引分片；CPU 保持在控制面；当跨卡归并不可避免时，优先采用 GPU 侧归并。这四项原则并非并列罗列，其中前两项决定主方案形态，后两项负责约束兜底路径和运行边界。

首先，查询级并行优于单查询跨卡并行。若一个查询必须在多张 GPU 上分别完成局部搜索并在末端归并，系统会额外承担局部结果表达、跨卡通信和同步等待等成本；而当不同查询被均衡分发到不同 GPU 时，每张设备就更容易复用单卡阶段已经压缩好的``候选召回---异常评分''闭环。cuVS 多 GPU 文档将 replicated 模式描述为通过在各 GPU 间分发查询以获得更高 query throughput，这一点与本文的吞吐目标一致\citep{cuvs_multi_gpu_nn}。

在此前提下，第二项原则进一步限定了主方案选择：数据并行与索引复制优先于一开始即采用索引分片。cuVS 的分布模式定义表明，replicated 更偏向吞吐优先，sharded 更偏向容量扩展\citep{cuvs_multi_gpu_nn,cuvs_dist_mode}。因此，只要索引仍可复制，完整副本在每张 GPU 上独立服务本地查询，通常比从一开始就引入跨卡依赖更符合本文的系统目标。

第三项原则主要约束控制边界，即 CPU 保持在控制面而不重新回到结果处理主路径。cuVS 文档提示，多 GPU ANN 的部分实现要求数据集、查询与输出数组位于 host memory，这意味着若直接沿用 host-centric 默认路径，则 CPU 与主机内存会重新进入关键路径\citep{cuvs_multi_gpu_ivf_host}。基于这一认识，本文在多 GPU 方案中将 CPU 定位为控制面调度者：它负责请求队列管理、负载均衡、bounded batching、运行时监控和最终小规模结果汇总，但不承担大规模候选集合或评分中间张量的重新组织。

最后一项原则只在主方案不能直接成立时才发挥作用，即当必须跨卡归并时，归并逻辑尽量下沉到 GPU 侧，并通过 NCCL 等设备间通信机制完成。NCCL 文档指出其通信操作会异步入队到 CUDA stream 上执行，但也警告在同一 \texttt{ncclGroupStart/End()} 中混用多个 stream 会形成全局同步点\citep{nccl_stream_semantics}。这意味着 GPU 侧归并虽然优于 CPU 聚合，却仍然带来额外通信组织成本，因此它在本文中是明确的兜底路径，而不是与 replicated 并行竞争的第二主线。

表~\ref{tab:primary-vs-fallback} 对 replicated 主方案、sharded+GPU 归并兜底方案以及 CPU 聚合对比路径的适用条件进行了归纳。

\begin{table}[htbp]
  \caption{多 GPU 扩展方案适用条件}
  \label{tab:primary-vs-fallback}
  \centering
  \small
  \begin{adjustbox}{max width=\linewidth}
    \begin{tabular}{P{1.9cm}P{2.8cm}P{4.0cm}P{3.2cm}P{3.0cm}P{1.8cm}}
      \toprule
      方案 & 核心思路 & 适用条件 & 优势 & 代价 & 定位 \\
      \midrule
      replicated &
      完整索引复制到每张 GPU，按查询级或微批级并行分发任务，每卡独立完成候选召回、异常评分与局部结果输出闭环 &
      单卡可容纳完整索引副本；系统目标以高吞吐和低尾延迟为主；允许用显存换取调度简单性和扩展稳定性 &
      数据平面最清晰；CPU 仅保留控制面职责；几乎不需要跨卡归并；与单卡优化链路复用度最高 &
      显存占用高；索引构建与更新需要维护多份副本；当索引继续增大时可扩展性受显存约束 &
      主方案 \\
      \midrule
      sharded+GPU 归并 &
      索引按卡分片，各卡先完成局部召回或局部评分，再通过 NCCL 等通信机制在 GPU 侧完成归并和最终结果合并 &
      完整索引无法复制到单卡；业务必须覆盖更大索引容量；可以接受额外的设备间通信与 GPU 侧归并复杂度 &
      支持更大索引规模；CPU 不再回收大规模候选集合；相较 CPU 聚合路径尾延迟更可控 &
      归并逻辑更复杂；通信开销与 stream 协调成本更高；性能更容易受到拓扑与 I/O 路径限制 &
      兜底方案 \\
      \midrule
      CPU 聚合 &
      各 GPU 仅输出局部结果，统一回传 CPU 进行排序、合并与最终输出，CPU 重新承担跨卡聚合职责 &
      主要用于原型验证、调试、小规模实验或需要显式检查中间结果的离线分析场景 &
      实现路径最直接；中间结果易于观察；便于快速构造对比基线 &
      CPU 重新进入数据平面；D2H/H2D 往返明显增加；高并发下 p99 延迟与吞吐表现最差 &
      对比基线 \\
      \bottomrule
    \end{tabular}
  \end{adjustbox}
\end{table}

\section{数据并行与索引复制的多 GPU 执行架构}

基于前述设计原则，本文将多 GPU 场景下的主扩展方案定义为数据并行与索引复制（replicated）。这一方案的核心取向非常明确：它优先解决吞吐扩展问题，而不是优先解决容量扩展问题。换言之，只要完整索引仍能复制到每张 GPU，本文就更倾向于用显存换取更简单的数据路径和更稳定的查询闭环，而不是过早引入跨卡依赖\citep{cuvs_multi_gpu_nn}。在该方案中，完整索引被复制到每张 GPU 上，每张 GPU 都具备完整的候选召回与异常评分能力；查询按批次或微批次被分发到不同 GPU，各设备在本地独立执行``查询向量组织---候选召回---异常评分---局部结果输出''全链路流程；CPU 主要承担请求级调度、批次分配和最终小规模结果汇总职责。

在执行流程上，数据并行与索引复制架构可以形式化表示为：给定总查询集合 $Q=\{q_1,q_2,\ldots,q_m\}$，调度器将其划分为若干子批次 $\{B_1,B_2,\ldots,B_g\}$，并将这些子批次分配给 $g$ 张 GPU。每张 GPU $G_j$ 本地维护一份完整索引 $\mathcal{I}_j=\mathcal{I}$，对其收到的批次 $B_j$ 独立执行
\begin{equation}
  C_j = \mathcal{R}(B_j,\mathcal{I}_j), \qquad S_j = \mathcal{F}(B_j,C_j),
\end{equation}
其中 $\mathcal{R}$ 表示候选召回，$\mathcal{F}$ 表示异常评分。由于每张 GPU 都能本地完成完整闭环，因此系统不需要在查询处理中途引入设备间依赖。

从系统工程角度看，索引复制虽然会增加显存占用，但换来了更低的执行复杂度和更清晰的数据路径。其优势主要体现在三个方面：查询之间天然独立，便于按请求或微批次做吞吐扩展；每张 GPU 都能沿用第四章已经优化好的本地执行链路，不需要为多卡场景重新拆解候选召回与评分接口；系统只需在极小规模结果层面做收口，而不需要在处理过程中持续维护跨卡依赖。也正因为如此，replicated 在本文中始终被定义为主方案，而不是若干方案中的普通选项。

图~\ref{fig:replicated-arch} 给出了 replicated 主方案的总体结构，直观展示了 CPU 控制面与 GPU 本地闭环在多卡场景下的分工关系。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig5_5_multi_gpu_overall_architecture.drawio.png}
  \caption{数据并行与索引复制多 GPU 架构}
  \label{fig:replicated-arch}
\end{figure}

\section{CPU 控制面调度与负载均衡}

在 replicated 架构下，CPU 的职责需要被严格限定。它负责的是“何时发、发给谁、怎样保持负载平衡”，而不是“替 GPU 整理中间结果”。因此，本节所讨论的控制面工作主要包括请求队列维护、微批形成、按 GPU 负载分发批次、控制 bounded batching 策略，以及在各 GPU 完成本地闭环之后接收最终小规模输出。

请求队列组织方面，系统采用统一入口队列接收查询请求，并根据队列长度、GPU 空闲度和微批策略动态形成批次。cuVS 动态批处理文档指出，动态批处理通过将小规模请求合并成批次，可在维持延迟可控的前提下提高设备占用率和吞吐率\citep{cuvs_dynamic_batching}。对本文而言，微批并不是单纯为了“批更大”，而是为了在吞吐和尾延迟之间建立一个可调的控制旋钮：批次过小会浪费设备并行能力，批次过大又会推高排队等待。

负载均衡方面，本文在 replicated 架构下优先采用查询级动态调度而非静态平均分配。cuVS 在 replicated 模式下提供了 \texttt{LOAD\_BALANCER} 与 \texttt{ROUND\_ROBIN} 两类搜索模式，其中前者用于保持各 GPU 负载均衡\citep{cuvs_dist_mode}。这类调度的意义并不只是“看起来分配得更平均”，而是为了防止某张 GPU 因瞬时队列堆积而拉高整机的 p99 延迟。

最终结果汇总方面，CPU 仅接收每张 GPU 已完成的最终小规模结果，如异常分数、Top-$k$ 排序结果和必要日志元信息。局部候选集合、评分中间张量以及归并前的设备侧结构都不应回流到 CPU。把职责边界限定在这里，有助于确保控制面增强的是调度能力，而不是重新演化为新的数据平面中心。

图~\ref{fig:control-plane} 展示了 CPU 控制面的调度与回收流程，表~\ref{tab:control-vs-data-plane} 汇总了控制面、数据平面、通信与 I/O 路径中的职责划分及关键参数。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig5_7_cpu_control_plane_microbatch.drawio.png}
  \caption{CPU 控制面调度与微批示意}
  \label{fig:control-plane}
\end{figure}

\begin{table}[htbp]
  \caption{控制面/数据面职责与关键参数}
  \label{tab:control-vs-data-plane}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{1.4pt}
  \renewcommand{\arraystretch}{1.22}
  \begin{adjustbox}{max width=\linewidth}
    \begin{tabular}{P{0.82cm}P{2.35cm}P{3.95cm}P{2.55cm}P{3.10cm}}
      \toprule
      \makecell[c]{类别} & \makecell[c]{组件} & \makecell[c]{职责} & \makecell[c]{关键\\参数} & \makecell[c]{备注} \\
      \midrule
      控制面 &
      \makecell[l]{请求队列与\\batching 调度器} &
      统一接收请求，执行 bounded batching，形成微批并控制批次进入时机 &
      {\scriptsize\makecell[l]{\texttt{batch\_size}\\\texttt{micro\_batch\_timeout\_ms}\\\texttt{max\_inflight\_batches}}} &
      \makecell[l]{直接决定吞吐与平均延迟的折中；\\CPU 只负责批次组织，\\不接收大规模候选集合} \\
      \midrule
      控制面 &
      GPU 负载均衡器 &
      按 GPU 空闲度、队列深度和显存水位动态分配微批 &
      {\scriptsize\makecell[l]{\texttt{queue\_depth\_threshold}\\\texttt{gpu\_mem\_watermark\_pct}\\\texttt{dispatch\_window}}} &
      \makecell[l]{优先保持各卡负载平衡并控制\\尾延迟；适用于 replicated 主路径} \\
      \midrule
      数据平面 &
      \makecell[l]{FlashANNS\\FAISS-GPU\\候选召回后端} &
      \makecell[l]{在设备侧完成向量检索、\\候选筛选与局部 top-$k$ 组织} &
      {\scriptsize\makecell[l]{\texttt{nprobe}\\\texttt{candidate\_k}\\\texttt{search\_stream\_count}}} &
      \makecell[l]{每张 GPU 独立执行本地召回；\\与单卡阶段的 GPU 常驻链路保持一致} \\
      \midrule
      数据平面 &
      \makecell[l]{PyTOD\\评分执行器} &
      \makecell[l]{在候选集合上执行张量化异常评分、\\局部排序和最终分数输出} &
      {\scriptsize\makecell[l]{\texttt{score\_batch\_size}\\\texttt{k}\\\texttt{score\_stream\_priority}}} &
      \makecell[l]{分数、排序结果和必要日志元信息\\留在设备侧直至最终输出} \\
      \midrule
      通信 &
      NCCL 归并与通信流 &
      在 sharded 兜底路径中完成 all-gather / reduce / merge 等设备间结果合并 &
      {\scriptsize\makecell[l]{\texttt{nccl\_op}\\\texttt{merge\_chunk\_size}\\\texttt{comm\_stream\_priority}}} &
      \makecell[l]{仅在 replicated 失效时启用；\\需避免在同一 \texttt{ncclGroupStart/End()}\\中混用多个 stream} \\
      \midrule
      I/O &
      \makecell[l]{I/O 队列与 lane\\准入控制器} &
      管理 SSD 读队列、GPU--SSD lane 绑定和 in-flight I/O 数量，抑制共享路径拥塞 &
      {\scriptsize\makecell[l]{\texttt{io\_queue\_depth}\\\texttt{max\_inflight\_io}\\\texttt{lane\_tokens\_per\_gpu}}} &
      \makecell[l]{与拓扑感知绑定策略联动；\\参数过大时会拉高 p99，\\过小则会限制吞吐} \\
      \bottomrule
    \end{tabular}
  \end{adjustbox}
\end{table}

\section{跨卡归并场景下的 GPU 侧归并与 NCCL 通信}

当索引容量超过单卡显存可容纳范围，或业务约束要求单一查询必须访问分布在多张 GPU 上的局部索引时，replicated 主方案就不再直接适用，系统必须进入跨卡归并场景。本文把这一场景明确界定为兜底路径：只有在主方案因容量或部署条件失效时，才引入 sharded 索引与跨卡结果合并。这样界定的原因很直接，因为一旦进入归并路径，系统就必须额外承担局部结果表达、设备间通信和流协调等成本。

在此条件下，本文仍坚持一个约束：局部结果尽量在 GPU 侧组织与合并，CPU 只接收最终极小规模结果。cuVS 在 sharded 模式下给出了 \texttt{MERGE\_ON\_ROOT\_RANK} 与 \texttt{TREE\_MERGE} 两类归并方式，为本文设计 GPU 侧归并路径提供了参考\citep{cuvs_dist_mode}。与 CPU aggregation 相比，GPU merge 的主要收益在于减少大规模结果回传和主机侧排序开销，使跨卡合并仍尽可能延续设备侧处理逻辑；但它并不会像 replicated 那样天然简单，因为额外的通信与同步组织是无法完全省掉的。

在设备间通信实现方面，本文采用 NCCL 作为主要通信机制，并将通信流与局部计算流明确分离。NCCL 文档指出其通信操作异步入队到指定 CUDA stream，但在同一 \texttt{ncclGroupStart/End()} 组内混用多个 stream 会形成全局同步点\citep{nccl_stream_semantics}。因此，GPU 侧归并的收益建立在一个前提上：通信必须被控制在必要范围内，并与本地计算流合理解耦。若通信组织不当，兜底方案很容易因同步点过多而侵蚀掉原本希望保留的扩展收益。

\section{拓扑感知 I/O lane 绑定与准入控制}

在多 GPU 异常检测系统中，扩展上限不仅由 GPU 数量决定，还受制于存储路径和 PCIe/NUMA 拓扑。单卡阶段被压缩掉的路径开销，在多卡环境下可能以另一种形式重新出现：如果多张 GPU 共享同一 SSD 通道或 PCIe 路径，局部检索和候选读取虽然仍在设备侧执行，但共享链路上的排队会直接反映到吞吐下降和 p99 上升上。GDS 文档指出，GDS 通过在 GPU memory 与 storage 之间建立 direct DMA path 避免 CPU bounce buffer，并强调拓扑路径会影响传输效率\citep{gds_overview,gds_design_guide}。这意味着，多 GPU 的实际收益往往先受到 I/O 路径约束，而不一定先受到计算约束。

基于这一认识，本文引入拓扑感知 I/O lane 绑定与准入控制机制：运行前分析 GPU、SSD、PCIe root complex、PCIe switch 与 NUMA 节点之间的关系；运行时尽量将 GPU 与 SSD 通道进行 lane 级绑定，并对每条 lane 的 in-flight 请求数做有界控制，以避免过度并发导致尾延迟失控。这里的重点不是解释硬件连接本身，而是把拓扑关系转化为可执行的资源组织策略，使“增加 GPU 数量”不会因为共享路径冲突而过早失效。

图~\ref{fig:topology} 以左右对比方式展示了拓扑感知 I/O lane 在合理绑定与共享冲突两种条件下的 GPU、SSD、PCIe 与 NUMA 关系，为后续扩展结果中的路径差异提供解释依据。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig5_11_topology_aware_io_lane.drawio.png}
  \caption{拓扑感知 I/O lane 示意}
  \label{fig:topology}
\end{figure}

\section{多 GPU 工程增强}

多 GPU 场景下，系统还需要工程增强机制保证运行稳定性与实验可重复性，包括多进程/多 GPU 运行时组织、统一监控指标、异常恢复与请求重试、以及资源隔离和日志管理。本节的作用不是引入新的扩展主线，而是为 replicated 主方案与 GPU merge 兜底路径提供统一的运行时支撑，使后续实验能够在可比口径下观测吞吐、延迟、CPU/GPU 利用率以及 I/O 路径占用。

\section{多 GPU 扩展实验与结果分析}

多 GPU 实验的规模口径统一按“真实集 $\rightarrow$ 1M 合成数据 $\rightarrow$ 10M 合成数据 $\rightarrow$ 50M 合成数据 $\rightarrow$ 100M 合成数据”组织。真实集和 1M 合成数据主要用于与有效性验证及桥接实验保持一致口径，10M 用于 replicated 主方案的起始扩展比较，50M 对应中间规模扩展观察，100M 则承担 GPU merge、CPU aggregation 以及共享路径冲突条件下的压力测试。围绕这一口径，本节重点回答三个设计问题：replicated 主方案是否能够在吞吐优先场景下获得稳定扩展；当主方案因容量约束失效时，GPU merge 作为兜底路径是否优于 CPU aggregation；在共享路径存在冲突时，拓扑感知 I/O lane 控制是否能够延缓多 GPU 扩展收益的退化。实验分别设置了 replicated 主方案、Local scoring + GPU merge 兜底方案、Local scoring + CPU aggregation 对比基线，以及 Shared-path conflict 路径冲突基线。实验关注 QPS、平均延迟与 p99 延迟、CPU 占用、GPU 利用率、设备间通信与 I/O 路径占用。

表~\ref{tab:multi-gpu-exp-setup} 给出了多 GPU 实验设置，图~\ref{fig:qps-vs-gpu} 与图~\ref{fig:latency-quantiles} 展示 replicated 扩展曲线以及不同归并路径下的延迟变化，图~\ref{fig:io-lane-impact} 用于比较 I/O lane 配置对扩展性的影响，表~\ref{tab:multi-gpu-summary} 对总体结果进行了汇总。需要说明的是，表图中展示的是该统一口径下的代表性结果：10M 主要对应 replicated 主方案的扩展曲线，100M 主要对应兜底路径与共享路径冲突场景。

从结果上看，replicated 方案在 1/2/4 GPU 条件下保持了较为稳定的 QPS 增长和相对平稳的延迟变化，说明在索引可复制的前提下，查询级并行和本地闭环执行确实更符合本文的吞吐优先目标。与之对应，Local scoring + GPU merge 和 Local scoring + CPU aggregation 的对比主要用于回答兜底路径如何选的问题：两者都建立在索引分片的前提下，但 GPU merge 在平均延迟、p99 延迟和 CPU 利用率上都明显优于 CPU aggregation，说明一旦必须跨卡归并，把局部结果留在设备侧处理仍然更有利于控制路径开销。

另一方面，I/O lane 绑定实验回答的是多 GPU 扩展收益为何不会无限线性增长。Topology-aware binding 与 Shared-path conflict 的对比表明，即便计算资源足够，当多张 GPU 共享同一 PCIe/SSD 路径时，QPS 仍会明显下降，p99 延迟也会迅速抬升。也就是说，多 GPU 扩展的实际表现并不只由归并方式决定，还受到共享路径组织方式的直接限制。

综合这些结果可以看到，本文在第五章给出的结构判断与实验现象是一致的：replicated 是吞吐优先场景下的主路径；GPU merge 仅在容量受限时作为更合适的兜底方案；CPU aggregation 保留为对比基线，用于说明主机侧归并会重新拉高路径成本；而拓扑感知 I/O 控制则决定了主方案和兜底方案各自能够把扩展收益保留到什么程度。

\begin{table}[htbp]
  \caption{多 GPU 实验设置}
  \label{tab:multi-gpu-exp-setup}
  \centering
  \small
  \begin{adjustbox}{max width=\linewidth}
    \begin{tabular}{P{0.8cm}P{2.6cm}P{1.0cm}P{1.9cm}P{1.0cm}P{1.0cm}P{0.7cm}P{2.2cm}P{1.8cm}P{2.7cm}}
      \toprule
      GPU数 & 方案 & 数据规模 & 索引组织 & 批大小 & 候选数 & $k$ & 归并路径 & I/O 绑定 & 备注 \\
      \midrule
      1 & Replicated & 10M & Full replica & 4096 & 128 & 50 & None & Topology-aware & 单卡吞吐扩展基线 \\
      2 & Replicated & 10M & Full replica & 4096 & 128 & 50 & None & Topology-aware & 2 GPU replicated 扩展基线 \\
      4 & Replicated & 10M & Full replica & 4096 & 128 & 50 & None & Topology-aware & 四卡 replicated 扩展基线 \\
      4 & Local scoring + GPU merge & 100M & Sharded index & 3072 & 128 & 50 & NCCL all-gather + GPU merge & Topology-aware & 显存受限时的 GPU 侧归并兜底方案 \\
      4 & Local scoring + CPU aggregation & 100M & Sharded index & 3072 & 128 & 50 & CPU aggregation & Topology-aware & CPU 聚合对比基线 \\
      2 & Topology-aware binding & 100M & Full replica & 4096 & 128 & 50 & None & Topology-aware & I/O lane 绑定对比实验 \\
      4 & Topology-aware binding & 100M & Full replica & 4096 & 128 & 50 & None & Topology-aware & I/O lane 绑定对比实验 \\
      2 & Shared-path conflict & 100M & Full replica & 4096 & 128 & 50 & None & Shared PCIe/SSD path & 共享路径冲突对比基线 \\
      4 & Shared-path conflict & 100M & Full replica & 4096 & 128 & 50 & None & Shared PCIe/SSD path & 共享路径冲突对比基线 \\
      \midrule
      \multicolumn{10}{p{15.6cm}}{\footnotesize 注：多 GPU 实验规模口径统一按“真实集 $\rightarrow$ 1M 合成数据 $\rightarrow$ 10M 合成数据 $\rightarrow$ 50M 合成数据 $\rightarrow$ 100M 合成数据”组织。表中列出的是正文展示所对应的代表性配置，其中 10M 主要服务于 replicated 主方案扩展曲线，100M 主要服务于兜底归并与路径冲突对比。} \\
      \bottomrule
    \end{tabular}
  \end{adjustbox}
\end{table}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.86\linewidth]{plot_assets/ch05_gpu_scaling_qps/fig5_15_qps_vs_gpu.png}
  \caption{QPS 随 GPU 数量变化}
  \label{fig:qps-vs-gpu}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.84\linewidth]{plot_assets/ch05_latency_merge_compare/fig5_16_latency_merge_compare.png}
  \caption{平均延迟与 p99 延迟变化}
  \label{fig:latency-quantiles}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{plot_assets/ch05_io_lane_impact/fig5_18_io_lane_impact.png}
  \caption{I/O lane 配置对扩展性的影响}
  \label{fig:io-lane-impact}
\end{figure}

\begin{table}[htbp]
  \caption{多 GPU 扩展结果汇总}
  \label{tab:multi-gpu-summary}
  \centering
  \small
  \begin{adjustbox}{max width=\linewidth}
    \begin{tabular}{P{0.8cm}P{2.6cm}P{1.0cm}P{1.6cm}P{1.8cm}P{1.8cm}P{1.6cm}P{1.6cm}}
      \toprule
      GPU数 & 方案 & 数据规模 & QPS & avg\_latency\_ms & p99\_latency\_ms & CPU利用率(\%) & GPU利用率(\%) \\
      \midrule
      1 & Replicated & 10M & 12640.00 & 10.80 & 16.20 & 34.00 & 69.00 \\
      2 & Replicated & 10M & 24760.00 & 9.10 & 14.60 & 30.50 & 75.50 \\
      4 & Replicated & 10M & 47620.00 & 8.40 & 12.90 & 26.50 & 82.00 \\
      4 & Local scoring + GPU merge & 100M & 40180.00 & 11.60 & 19.40 & 32.00 & 78.00 \\
      4 & Local scoring + CPU aggregation & 100M & 28940.00 & 15.80 & 28.60 & 49.00 & 64.50 \\
      2 & Topology-aware binding & 100M & 24780.00 & 11.40 & 18.40 & 31.00 & 74.00 \\
      4 & Topology-aware binding & 100M & 47560.00 & 13.20 & 22.10 & 29.00 & 80.00 \\
      2 & Shared-path conflict & 100M & 21920.00 & 14.80 & 24.80 & 36.00 & 67.50 \\
      4 & Shared-path conflict & 100M & 33140.00 & 24.20 & 41.60 & 43.00 & 58.00 \\
      \midrule
      \multicolumn{8}{p{13.0cm}}{\footnotesize 注：结果展示遵循与表~\ref{tab:multi-gpu-exp-setup} 相同的多 GPU 统一规模口径。受版面与结果表达重点限制，表中列出的是 10M replicated 扩展结果和 100M 压力场景结果，用于回答主方案、兜底路径与共享路径冲突三类设计问题。} \\
      \bottomrule
    \end{tabular}
  \end{adjustbox}
\end{table}

\section{本章小结}

本章的工作主要形成了三项进入后续综合实验的结构性结论。第一，在索引可复制的前提下，replicated 能够最直接地复用第四章已经优化好的单卡执行链路，因此是吞吐优先场景下的主扩展方案。第二，当容量约束迫使系统进入分片路径时，GPU merge 相比 CPU aggregation 更能保留设备侧处理优势，但其收益仍然受通信与同步组织方式影响。第三，多 GPU 扩展的上限不仅取决于计算资源，还取决于 I/O 路径与拓扑关系能否支撑多卡并发。

这些结论在本章内完成了初步验证，但其适用边界还需要在下一章与单卡优化结果、不同数据规模表现和整体检测效果一起统一考察。因此，第六章将不再分别讨论单卡与多卡的局部机制，而是把本章得到的主方案、兜底路径和路径冲突基线放回到综合实验框架中，继续验证其在整体系统层面的有效性与边界。
